

sub OpenCycHTNModule {
  # Other important thinking, I cannot begin work on certain projects,
  # for instance, the meetup client, until CORPUS is finished.  We can
  # represent this as an HTN in opencyc.
}
